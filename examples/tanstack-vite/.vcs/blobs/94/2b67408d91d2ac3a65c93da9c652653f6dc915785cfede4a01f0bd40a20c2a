import { useQuery } from '@tanstack/react-query'

export function Feature3() {
  const { data } = useQuery({ queryKey: ['feature', 3], queryFn: async () => 3 })
  return <div>Feature 3: {data}</div>
}
